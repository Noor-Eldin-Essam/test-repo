name: Deploy Static Site

on: push

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # Ensure tokens.js is ignored by Git (if not already)
      - name: Add tokens.js to .gitignore
        run: |
          echo "tokens.js" >> .gitignore
        shell: bash

      # Remove old tokens file (if exists)
      - name: Remove old tokens file (if exists)
        run: |
          if [ -f tokens.js ]; then
            echo "Removing old tokens.js file"
            rm tokens.js
          else
            echo "No existing tokens.js file to remove"
          fi
        shell: bash

      # Create new tokens.js file only during build
      - name: Create new tokens file for deployment
        run: |
          echo "export const validTokens = ['${{ secrets.SECRET_API_TOKEN }}'];" > ./tokens.js
          echo "New tokens.js file created for deployment:"
          cat tokens.js
        shell: bash

      # List files after creating tokens.js
      - name: List files after creating tokens.js
        run: |
          ls -la
        shell: bash

      # List files before deployment
      - name: List files before deployment
        run: |
          pwd
          ls -la
        shell: bash

      # Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .  # Update to your build output directory if needed
          destination_dir: .

      # Verify deployment (optional)
      - name: Verify deployment
        run: |
          echo "Deployment completed"
        shell: bash
