name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '14'

      - name: Replace placeholder with SECRET_API_TOKEN
        run: |
          sed -i 's/{{SECRET_API_TOKEN}}/'"$SECRET_API_TOKEN"'/g' ./index.html
        env:
          SECRET_API_TOKEN: ${{ secrets.SECRET_API_TOKEN }}

      - name: Deploy to Hosting
        run: |
          echo "Deploying..."
          # Deployment script here
