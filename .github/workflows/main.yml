name: Deploy Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Replace placeholders with SECRET_API_TOKEN
        run: |
          grep -rl '{{SECRET_API_TOKEN}}' . | xargs sed -i 's/{{SECRET_API_TOKEN}}/'"$SECRET_API_TOKEN"'/g'
        env:
          SECRET_API_TOKEN: ${{ secrets.SECRET_API_TOKEN }}

      - name: Deploy to Hosting
        run: |
          echo "Deploying..."
          # Deployment script here
